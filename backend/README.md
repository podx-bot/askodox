# ASKODOX Backend

Canonical backend location for the ASKODOX monorepo.

Migration source: `podx-bot/podx-ai-connect-v2` commit `915327953098ab64ecedcfd9553580bbf48f8af1`.

Railway must stay on the current root bridge until the complete `backend/app` source is vendored and verified. After verification, set Railway Root Directory to `/backend` and remove the temporary root bridge.

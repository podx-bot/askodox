import 'package:flutter/material.dart';import 'package:flutter_riverpod/flutter_riverpod.dart';import '../../../core/providers/backend_providers.dart';
class SyncStatusScreen extends ConsumerStatefulWidget{const SyncStatusScreen({super.key});@override ConsumerState<SyncStatusScreen> createState()=>_State();}class _State extends ConsumerState<SyncStatusScreen>{@override Widget build(BuildContext context){final sync=ref.watch(syncServiceProvider);final status=sync.status;return Scaffold(appBar:AppBar(title:const Text('Data synchronization')),body:ListView(padding:const EdgeInsets.all(24),children:[Text('Pending changes: ${status.pendingCount}'),Text('Failed items: ${status.failedCount}'),Text('Last synced: ${status.lastSyncedAt?.toIso8601String()??'Never'}'),const SizedBox(height:24),FilledButton.icon(onPressed:()async{await sync.sync();if(mounted)setState((){});},icon:const Icon(Icons.sync),label:const Text('Sync now'))]));}}
